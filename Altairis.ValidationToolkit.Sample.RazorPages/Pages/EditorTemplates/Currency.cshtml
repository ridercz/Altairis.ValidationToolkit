@{
    // Set layout to empty to avoid conflict with parent layout
    this.Layout = string.Empty;

    ViewData["attr-step"] = "any";
    ViewData["additionalCssClass"] = "currency";
}
<partial name="_Number.cshtml" />
